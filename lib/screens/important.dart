import 'package:flutter/material.dart';

class ImportantScreen extends StatelessWidget {
  const ImportantScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ImportantScreen'),
      ),
      body: Container(
        child: Text("Importanttt ssss"),
      ),
    );
  }
}
